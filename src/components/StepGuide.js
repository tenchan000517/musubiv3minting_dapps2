import React from 'react';

function StepGuide({ step }) {
  return (
    <div>
      <h2>Step {step}</h2>
      {/* Add the content for each step here */}
    </div>
  );
}

export default StepGuide;
